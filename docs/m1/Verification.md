# Module 1 verification record

Date: 2026-09-09. Workspace: Resume Builder. All data below is synthetic/local; the project database and provider accounts were not used.

## Verified API behavior

Command: npm test in server. Runtime: Node 22.13.1, npm 10.9.2, isolated MongoDB 8.2.6 via mongodb-memory-server. Final API run: **18 tests passed, 0 failed**, approximately 115 seconds.

The scenarios cover required configuration rejection; import without AI keys; normalized registration/login and password hashing; concurrent duplicate registration; invalid credentials/input/protected fields; CSRF/origin/fetch metadata; persistent profile preferences; logout/expiry/deleted-user denial; password/session revocation; hashed one-time recovery and concurrent replay rejection; private file ownership/download headers; traversal/out-of-storage sentinel protection; inactive-file denial; MIME/signature/malformed/oversized multipart rejection; shared rate limits across app instances; missing-provider/scanner containment; readiness/error redaction; secure production cookie flags; three global AI slots and one per user; Vercel API/SPA routing generation; valid OOXML parsing and oversized DOCX metadata rejection.

Production cookie flags were tested in the app with production configuration, **not through a deployed Vercel edge**. Provider concurrency was exercised with a controlled local responder, with no real AI requests. The valid DOCX was a minimal genuine OOXML ZIP generated by the test.

Initial attempts are not counted as passes: the sandbox blocked the MongoDB test cache; the first real run passed 11/14 and exposed an overstrict multipart part count. Those failures were corrected before the 18/18 run. Another intermediate run overlapped dependency replacement and failed to import a temporarily missing package; final verification ran after dependencies settled.

## Dependencies and compilation

Both updated npm dependency trees reported **0 advisories** after compatible updates and the necessary Express 5 / React Router 7 upgrades. Multer is 2.3.0; Puppeteer 25.10.0; Express 5.2.1; React Router 7.18.3. Unused dependencies were removed. Zero npm findings is not a claim that all application vulnerabilities are fixed.

Client lint passed with no diagnostics. The production build passed with Vite 7.3.6 and 126 transformed modules. Final build after metadata/token updates passed: HTML 0.63 kB; CSS 40.68 kB (gzip 7.16 kB); JavaScript 364.38 kB (gzip 114.13 kB). Client lint passed. Documentation validation found no broken local links/errors across its 12-document set, and git diff --check passed after formatting cleanup. Source_Manifest.json records 84 source/configuration hashes for the final working tree. The build required execution outside the sandbox because esbuild could not read the Vite configuration through the restricted environment.

## Browser verification — local scenarios passed

Playwright + Chromium, desktop 1440×1000 and mobile 360×800, using a separate ephemeral API/database. No production .env is loaded.

The first browser fixture timed out while downloading a second MongoDB binary. The fixture now explicitly reuses the server test binary cache. The first completed 8-scenario browser run passed 4 and failed 4: two registration assertions expired while the UI was still loading, the mobile auth page lacked an h1, and Tab could leave the recovery dialog. The heading and focus trap were corrected, and the registration wait was bounded at 15 seconds. The clean full rerun passed six scenarios; both recovery scenarios still exceeded their original five-second dialog wait. Tests now await the actual recovery HTTP response within 20 seconds, then assert the dialog and reset flow. The targeted desktop/mobile recovery rerun passed both scenarios (26.8s and 45.1s). **All eight browser scenarios have passed across the full run and targeted rerun.** The flow timeout remains bounded at 90 seconds; this machine's test timings are not a production latency benchmark.

Screenshots contain synthetic data. Reviewed images: login-desktop.png, login-mobile.png, settings-desktop.png and settings-mobile.png. They show the corrected shared UI. Axe reported no violations on the tested auth/settings/dialog states. This is not full-platform or manual screen-reader certification.

## Remaining module/release gates

- Actual Vercel/API/Mongo provisioning, API rewrite and secure cookie behavior through the edge.
- Trusted proxy/client-IP attribution and deployed two-user cache isolation.
- Remote CI execution and Docker runtime proof (configuration exists; no claim of a run).
- Broader UI journeys and manual screen-reader/zoom testing, scoped with each later feature module.
- M2 private durable storage, lifecycle/retention and comprehensive PDF/DOCX parsing quality.
- M3/M4 truthful AI output, persisted analysis/drafts and complete exports.
- M5 real discovery with a durable bounded worker. Scanner containment is temporary; all-feature launch scope is unchanged.
- M6 backup/restore, monitoring, rollback and launch acceptance.

Module 1 implementation and local verification are complete. It is not yet marked technically accepted or owner approved because the staging/operational gate remains open. Module 2 has not started.
